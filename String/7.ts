const string = "hesoyam";

const arrayString = string.split('');
const firsElement = arrayString[0];
const lastElement = arrayString[arrayString.length - 1];
console.log(firsElement.charCodeAt(0), lastElement.charCodeAt(0));